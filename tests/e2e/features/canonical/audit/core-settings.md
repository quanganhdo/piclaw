# Core Settings audit

Scope notes:
- Focus: Classic Settings dialog and directly cited owner-bound settings routes.
- Non-goal: claim all skins expose identical controls. Visual parity is cited only where a test explicitly proves shared behavior.
- Access modes: Budget is explicitly single-user-only; restricted multi-user modes must stay denied rather than simulated.

| Tag | Scenario | Source path + symbol | Existing tests | Pending gaps |
| --- | --- | --- | --- | --- |
| `@ux-settings-001` | Open Settings once and dismiss it without activating the workspace underneath | `runtime/web/src/components/settings-dialog.ts` :: `SettingsDialog`, `SettingsDialogContent`, `openSettingsDialog` | `tests/e2e/features/classic/settings/settings-dialog.feature` :: `Rapid shortcut presses open exactly one settings dialog`; `tests/e2e/features/classic/settings/settings-layering.feature` :: `Only settings dialog is interactive above the backdrop`; `tests/e2e/steps/us24-hamburger-menu-items.spec.ts` :: `workspace header menu exposes Settings and opens the settings dialog` | No canonical source-backed scenario currently unifies keyboard open, workspace-menu open and safe dismissal. |
| `@ux-settings-002` | Cold-open Settings shows a shell immediately and then resolves General | `runtime/web/src/components/settings-dialog.ts` :: `_settingsDataCache`, `renderSectionLoading`, `SettingsDialogContent` | `tests/e2e/features/classic/settings/settings-dialog.feature` :: `Settings shows loading shell then content`; `tests/e2e/steps/us06-settings.spec.ts` :: `settings opens within acceptable time` | No canonical scenario ties the loading shell to the shared settings-data fetch and General-first resolution. |
| `@ux-settings-003` | General is preloaded and other built-in sections lazy-load on first visit | `runtime/web/src/components/settings-dialog.ts` :: `builtinSectionComponentCache`, `BUILTIN_SECTION_LOADERS`, `loadBuiltinSection` | `tests/e2e/features/classic/settings/settings-dialog.feature` :: `Non-General panes load only on click`; `tests/e2e/steps/us06-settings.spec.ts` :: `non-General panes load only on click` | No existing test asserts cached revisit behavior for an already loaded non-General section. |
| `@ux-settings-004` | Searchable sections focus the header filter and responsive widths change layout classes only | `runtime/web/src/components/settings-dialog.ts` :: `filterRef`, `sectionPlaceholder`, `layoutMode` | `runtime/test/web/model-settings-components.test.ts` :: `Settings layouts become single-column with 44px controls on phones`; `runtime/test/web/model-settings-components.test.ts` :: `classic and visual Settings use the shared bounded master-detail catalogue` | No Classic E2E located for section-search autofocus or compact/narrow class transitions at the dialog shell. |
| `@ux-settings-005` | Compaction exposes dense aligned controls for automatic and manual policies | `runtime/web/src/components/settings/compaction.ts` :: `CompactionSection` | `tests/e2e/steps/us30-compaction-settings.spec.ts` :: `settings has compaction/context controls`; `tests/e2e/steps/us30-compaction-settings.spec.ts` :: `processing method selector persists the canonical Pipelined value`; `runtime/test/web/settings-row-alignment.test.ts` :: `Classic Compaction opts into dense rows without changing shared Settings rows`; `runtime/test/web/compaction-settings-handler.test.ts` :: `saveCompactionSettings persists and applies compaction settings immediately` | No E2E located for unavailable configured compaction-model repair state or the inline probe action. |
| `@ux-settings-006` | Compaction surfaces watchdog state, active suppressions and per-chat reset actions | `runtime/web/src/components/settings/compaction.ts` :: `CompactionSection`, `resetBackoff` | `runtime/test/web/compaction-settings-handler.test.ts` :: `saveCompactionSettings can disable watchdog without clearing its timeout`; `runtime/test/web/compaction-settings-handler.test.ts` :: `getCompactionSettingsData exposes active backoffs and tracked phases and reset clears them` | No Settings-pane E2E located for visible suppression rows, phase rows or reset-action UX. |
| `@ux-settings-007` | Providers expose only the supported setup controls for each provider | `runtime/web/src/components/settings/providers.ts` :: `ProvidersSection`, `setupApiKey`, `setupCustom`, `startOAuth`, `logout` | `runtime/test/channels/web/dispatch-agent-provider-settings.test.ts` :: `marks custom model providers as configured from models.json` | No Classic UI test located for OAuth vs API-key vs custom-form branches, or configured-state logout/reconfigure UX. |
| `@ux-settings-008` | Models loads and mutates the authoritative catalogue for the current chat | `runtime/web/src/components/settings/models.ts` :: `resolveModelsSettingsChatJid`, `sendModelsSettingsCommand`, `loadModels`, `setScopedModels` | `tests/e2e/steps/us30-compaction-settings.spec.ts` :: `model scope filter available in settings`; `runtime/test/web/model-settings-chat-affinity.test.ts` :: `Models settings resolves the active branch chat for model/thinking commands`; `runtime/test/web/model-settings-chat-affinity.test.ts` :: `Models settings sends thinking changes to the active branch chat` | No end-to-end Classic test located for stale-result suppression when the active chat changes mid-load. |
| `@ux-settings-009` | Models keeps a bounded grouped master-detail catalogue with keyboard navigation | `runtime/web/src/components/settings/models.ts` :: `handleListKeyDown`, `renderEntry`; `runtime/web/src/ui/model-settings-catalogue.ts` :: `buildModelSettingsProjection`, `collectModelSettingsFacets`, `moveModelSettingsActiveKey` | `runtime/test/web/model-settings-catalogue.test.ts` :: `Settings projection bounds 405 models and keeps a matching selected row addressable`; `runtime/test/web/model-settings-catalogue.test.ts` :: `Settings projection uses shared filters, ranking and facets`; `runtime/test/web/model-settings-catalogue.test.ts` :: `Settings list navigation has one active key and supports paging`; `runtime/test/web/model-settings-components.test.ts` :: `Settings only scrolls selected rows after explicit keyboard navigation` | No Classic E2E located for the full listbox keyboard flow with grouped results and pin-button non-selection semantics. |
| `@ux-settings-010` | Models actions stay truthful about compatibility, confirmation and next steps | `runtime/web/src/components/settings/models.ts` :: `switchModel`, `setThinkingLevel`, `compactContext` | `runtime/test/web/model-settings-components.test.ts` :: `confirmed Settings switches synchronise badges before context refresh`; `runtime/test/web/model-settings-components.test.ts` :: `Settings treats command-level model, thinking and compaction failures as errors`; `runtime/test/web/model-settings-catalogue.test.ts` :: `Settings detail never exposes actions for a model hidden by filters` | No Classic UI test located for blocked-model `Compact context` CTA or the in-pane `Provider settings` navigation button. |
| `@ux-settings-011` | Budget stays owner-bound, single-user-first and explicit about task-budget ownership | `runtime/web/src/components/settings/budget.ts` :: `BudgetSection`; `runtime/src/channels/web/handlers/budget-settings.ts` :: `handleBudgetSettingsRead`, `handleBudgetSettingsAction` | `runtime/test/web/budget-settings-ui.test.ts` :: `shared budget model derives provider metric and requires explicit revision on edits`; `runtime/test/channels/web/http-dispatch-agent-budget-settings.test.ts` :: `creates and revises caps only with the current explicit revision`; `runtime/test/channels/web/http-dispatch-agent-budget-settings.test.ts` :: `grants bounded allowance and warnings-only with continuation guidance`; `runtime/test/channels/web/http-dispatch-agent-budget-settings.test.ts` :: `resumes and cancels only the current chat work`; `runtime/test/channels/web/http-dispatch-agent-budget-settings.test.ts` :: `denies reads and mutations outside single-user mode before parsing` | No Settings-pane E2E located for current-work actions or the restricted-mode absence/denial story. |
| `@ux-settings-012` | Scheduled Tasks lists supported tasks and owns per-run task budget edits | `runtime/web/src/components/settings/scheduled-tasks.ts` :: `ScheduledTasksSection`, `labelForSchedule`; `runtime/web/src/components/settings/budget.ts` :: `requestBudgetSettingsSection` callsite | `runtime/test/channels/web/http-dispatch-agent-scheduled-tasks.test.ts` :: `lists scheduled tasks with counts for the settings pane`; `runtime/test/channels/web/http-dispatch-agent-scheduled-tasks.test.ts` :: `pauses and resumes non-internal scheduled tasks`; `runtime/test/channels/web/http-dispatch-agent-scheduled-tasks.test.ts` :: `edits per-run budget only through the scheduled task surface`; `runtime/test/channels/web/http-dispatch-agent-scheduled-tasks.test.ts` :: `rejects per-run budgets for shell tasks` | No Classic Settings E2E located for detail-pane task actions or the Budget→Scheduled Tasks handoff. |
| `@ux-settings-013` | Environment supports refresh and overrides while protecting keychain-injected names | `runtime/web/src/components/settings/environment.ts` :: `EnvironmentSection`; `runtime/src/environment-overrides.ts` :: `getEnvironmentSettingsData`, `setEnvironmentOverride`, `clearEnvironmentOverride` | `runtime/test/environment-overrides.optional.test.ts` :: `lists process environment variables and omits keychain-injected names`; `runtime/test/environment-overrides.optional.test.ts` :: `persists overrides in extension KV and applies them to process.env`; `runtime/test/environment-overrides.optional.test.ts` :: `rejects keychain-injected names as overrides` | No Settings-pane E2E located for add/save/clear override flows. |
| `@ux-settings-014` | Keychain keeps secrets encrypted, searchable and gated before reveal | `runtime/web/src/components/settings/keychain.ts` :: `KeychainSection`, `handleAdd`, `handleSaveNotes`, `handleRevealClick`, `handlePasswordSubmit`, `handleTotpSubmit`, `handleDelete` | No Classic settings-pane tests located; related backend coverage exists in `runtime/test/keychain.test.ts` | Missing direct UI coverage for reveal gating, notes save, copy actions and delete-confirmation UX in the Settings pane. |
| `@ux-settings-015` | Add-ons support filtered catalogue actions and extension-pane registration without claiming skin parity | `runtime/web/src/components/settings/addons.ts` :: `AddonsSection`; `runtime/web/src/components/settings/pane-registry.ts` :: `registerSettingsPane`; `runtime/web/src/components/settings-dialog.ts` :: extension section merge | `runtime/test/web/addon-web-extensions.test.ts` :: `addon web API exposes the current chat jid to add-on settings panes`; `runtime/test/web/addon-web-extensions.test.ts` :: `addon web registries support workspace panes, settings panes, standalone URLs, and attachment previews`; `runtime/test/web/settings-pane-order-parity.test.ts` :: `both skins sort add-on panes by label then id and ignore legacy order` | No Classic E2E located for add-on install/update/remove/restart flows from the Settings pane. |
| `@ux-settings-016` | Keyboard settings filters and edits shortcut bindings through the shared shortcut model | `runtime/web/src/components/settings/keyboard.ts` :: `KeyboardSection`; `runtime/web/src/ui/keyboard-shortcut-settings.ts` | `runtime/test/web/keyboard-shortcut-settings.test.ts` :: `reads, filters, saves and resets the same drafts for both skins`; `runtime/test/web/keyboard-settings-parity.test.ts` :: `classic Keyboard uses the shared settings model and no layout-critical inline styles`; `runtime/test/web/app-browser-events.test.ts` :: `watchSettingsShortcut accepts default bindings and ignores editable targets` | No Classic Settings E2E located for per-row save/default/reset-all shortcut editing. |
| `@ux-settings-017` | Workspace settings own terminal, direct-VNC and tree scan preferences | `runtime/web/src/components/settings/workspace.ts` :: `WorkspaceSection`; `runtime/src/channels/web/handlers/workspace-settings.ts` | `runtime/test/web/workspace-settings-handler.test.ts` :: `saveWorkspaceSettings persists and applies workspace settings immediately`; `runtime/test/web/settings-save-state.test.ts` :: `workspace save responses replace nested workspace settings and preserve unrelated data`; `runtime/test/web/workspace-tree-cache-settings.test.ts` :: `WorkspaceTreeCache clamps requested depth to configured workspace max depth` | No Classic UI test located for terminal/VNC toggles or numeric workspace controls inside Settings. |
| `@ux-settings-018` | Appearance settings apply theme preset, custom tint and output padding from one section | `runtime/web/src/components/settings/appearance.ts` :: `ThemeSection`, `applyLocal`; `runtime/src/channels/web/handlers/general-settings.ts` :: appearance persistence payload | No direct Settings-pane tests located; related theme/tint command coverage exists in `tests/e2e/steps/theme-tint-commands.spec.ts` | Missing Settings-pane coverage for preset selection, tint clear/apply, output-pad save and merge-back behavior. |

## Continuation evidence

All new rows were source checked; independent review covered 019–030. Rows 031–032 have coordinator source review only. No browser execution. Existing tests are related coverage, not per-clause proof.

| IDs | Source (repository-relative) and symbols | Existing related evidence |
|---|---|---|
| ux-settings-019, ux-settings-020, ux-settings-021, ux-settings-022, ux-settings-030 | `runtime/web/src/components/settings/general.ts` — GeneralSection; resolveAvatarPreview; writeSettingsClipboardText | general-settings-ui.test.ts; general-settings-handler.test.ts |
| ux-settings-023 | `runtime/web/src/components/settings/sessions.ts` — SessionsSection | general-settings-handler.test.ts (route coverage only) |
| ux-settings-024, ux-settings-025, ux-settings-026 | `runtime/web/src/components/settings/recordings.ts` — RecordingsSection; load; selectRecording; start; stop; remove; runPreview | No direct browser assertions located |
| ux-settings-027, ux-settings-028 | `runtime/web/src/components/settings/tools.ts` — ToolsSection; toggleGroup; toggleSearchMode; toggleToolCompaction | No direct browser assertions located |
| ux-settings-029 | `runtime/web/src/components/settings/quick-actions.ts` — QuickActionsSection; load; resetDefaults; save | timeline-quick-actions-settings.test.ts |
| ux-settings-031 | `runtime/web/src/components/settings/editor-settings.ts` — EditorSettingsSection; registerSettingsPane | No direct browser assertions located |
| ux-settings-032 | `runtime/web/src/components/settings/developer-settings.ts` — DeveloperSettingsSection; registerSettingsPane | No direct browser assertions located |

Meters also uses `runtime/web/src/ui/meters.ts` (`applyMetersEnabled`, `persistMetersState`). Keychain 014 now follows server-directed success/password/TOTP responses; independent review confirmed the correction. The earlier table's “No canonical scenario” statements described input gaps now filled by this feature, not missing current rows.
