diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index c5a9f5da3f822a601091bb2ee8d2a30db41dd422..fcb7b1fee82dfca0c5988beee9f7f554f801faee 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -1105,8 +1105,10 @@ function convertTools(tools, compat) {
 }
 function parseChunkUsage(rawUsage, model) {
     const promptTokens = rawUsage.prompt_tokens || 0;
-    const cacheReadTokens = rawUsage.prompt_tokens_details?.cached_tokens ?? rawUsage.prompt_cache_hit_tokens ?? 0;
-    const cacheWriteTokens = rawUsage.prompt_tokens_details?.cache_write_tokens || 0;
+    const cacheReadCandidates = [rawUsage.prompt_tokens_details?.cached_tokens, rawUsage.prompt_cache_hit_tokens];
+    const cacheReadTokens = cacheReadCandidates.find((value) => typeof value === "number" && Number.isFinite(value)) ?? 0;
+    const cacheWriteValue = rawUsage.prompt_tokens_details?.cache_write_tokens;
+    const cacheWriteTokens = typeof cacheWriteValue === "number" && Number.isFinite(cacheWriteValue) ? cacheWriteValue : 0;
     // Follow documented OpenAI/OpenRouter semantics: cached_tokens is cache-read
     // tokens (hits). OpenAI does not document or emit cache_write_tokens, but
     // OpenRouter-compatible providers can include it as a separate write count.
@@ -1123,8 +1125,13 @@ function parseChunkUsage(rawUsage, model) {
         output: outputTokens,
         cacheRead: cacheReadTokens,
         cacheWrite: cacheWriteTokens,
+        cacheReadReported: cacheReadCandidates.some((value) => typeof value === "number" && Number.isFinite(value)),
+        cacheWriteReported: typeof cacheWriteValue === "number" && Number.isFinite(cacheWriteValue),
         reasoning: rawUsage.completion_tokens_details?.reasoning_tokens || 0,
         totalTokens: input + outputTokens + cacheReadTokens + cacheWriteTokens,
+        ...(typeof rawUsage.cost === "number" && Number.isFinite(rawUsage.cost) && rawUsage.cost >= 0
+            ? { providerCost: rawUsage.cost }
+            : {}),
         cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, total: 0 },
     };
     calculateCost(model, usage);
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 77b46da2d31e472e519772b0d739766fa86dc3d0..05c635bcb3a29d929b8d9cf4c1ce6fabdf24ef47 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -257,6 +257,10 @@ export interface Usage {
     output: number;
     cacheRead: number;
     cacheWrite: number;
+    /** Whether the provider response explicitly included a cache-read token field, including an explicit zero. */
+    cacheReadReported?: boolean;
+    /** Whether the provider response explicitly included a cache-write token field, including an explicit zero. */
+    cacheWriteReported?: boolean;
     /** Subset of `cacheWrite` written with 1h retention. Only Anthropic reports this split. */
     cacheWrite1h?: number;
     /**
@@ -266,6 +270,8 @@ export interface Usage {
      */
     reasoning?: number;
     totalTokens: number;
+    /** Total request charge reported directly by the provider, when available. */
+    providerCost?: number;
     cost: {
         input: number;
         output: number;
